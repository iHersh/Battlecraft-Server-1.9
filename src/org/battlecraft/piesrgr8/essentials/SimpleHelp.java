package org.battlecraft.piesrgr8.essentials;

public class SimpleHelp{
/*	
	private BattlecraftServer plugin;
	
	public SimpleHelp(BattlecraftServer p) {
		this.plugin = p;
	}
	
	@Override
	public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args) {
		for (String list : plugin.getConfig().getStringList("helpblockmessages")) {
				if (!sender.hasPermission("bc.helpbypass")) {
				sender.sendMessage(ChatColor.translateAlternateColorCodes('&', list));
			}
		}
		return true;
	} */
}